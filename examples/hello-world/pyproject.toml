[project]
name = "hello-world"
version = "0.1.0"
description = "A PAIS plugin"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
