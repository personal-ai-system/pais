# PAIS Configuration (development/example)
# Personal AI System
# Copy to ~/.config/pais/pais.yaml for personal use

# Log level: trace, debug, info, warn, error, off
# Can be overridden by RUST_LOG env var
log-level: info

paths:
  plugins: ~/.config/pais/plugins
  skills: ~/.config/pais/skills
  history: ~/.config/pais/history

hooks:
  security-enabled: true
  history-enabled: true
  ui-enabled: true

observability:
  enabled: true
  sinks:
    - file

environment:
  repos-dir: ~/repos/

  tool-preferences:
    ls: eza
    tree: eza --tree
    grep: rg
    find: fd
    cat: bat

  tools:
    otto:
      github: otto-rs/otto
      description: CI runner, task automation
    clone:
      github: scottidler/clone
      description: Git clone to repos-dir/{org}/{repo}
